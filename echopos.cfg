log_flags Console +DoNotEcho | grep ~

setinfo "mestxt" setpos_exact | grep ~
setinfo "cmd;echoln" = | grep ~
setinfo "cmd;echoln" = | grep ~

getpos_exact | toggle "mestxt"
echoln [coord1]
mestxt | toggle "cmd;echoln"
getpos_exact | toggle "mestxt"
echoln [coord2]
mestxt | toggle "cmd;echoln"

log_flags Console -DoNotEcho | grep ~